import type { NextPage } from 'next';
import Typography from '@mui/material/Typography';

const Home: NextPage = () => (
  <Typography variant="h1">
    Homepage works!
  </Typography>
);

export default Home;
